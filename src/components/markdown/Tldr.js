export default function Tldr({ children }) {
  return (
    <>
      {children}
    </>
  );
}

// TODO: Create a TLDR box that takes a list of bullets to show
//  Consider including a link to relevant sections